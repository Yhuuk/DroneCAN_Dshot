# 使用CMSIS-DAP调试器。
source [find interface/cmsis-dap.cfg]

# CMSIS-DAP通过SWD接口连接STM32。
transport select swd

# STM32L431属于STM32L4系列。
source [find target/stm32l4x.cfg]

# 先使用较保守的1 MHz调试速度，保证连接稳定。
adapter speed 1000